# needed to fix GPG commit signing
export GPG_TTY=$(tty)

export EDITOR=/bin/nvim
export VISUAL=/bin/nvim

export PIP_REQUIRE_VIRTUALENV=true
export ZSH=$HOME/.oh-my-zsh

# Go env variables
export GOPATH=$HOME/.go
export GOBIN=$HOME/.local/bin
